language: objective-c
before_install:
gem install cocoapods
pod install --project-directory=Example
script:
- set -o pipefail && xcodebuild test -workspace Example/DORDoneHUD.xcworkspace -scheme DORDoneHUD-Example -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
